import Layout from '../layout/layout';

export default class Graffiti {

    constructor(config) {
        this.$config = config;
        this.$functions = {};
        this.$activatedFunction = {};
    }
}