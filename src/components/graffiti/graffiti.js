import Layout from '../layout/layout';

export default class SVGraffiti {

    constructor(config) {
        this.$config = config;
        this.$functions = {};
        this.$activatedFunction = {};
    }
}